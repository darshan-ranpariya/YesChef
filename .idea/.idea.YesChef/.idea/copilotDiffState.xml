<?xml version="1.0" encoding="UTF-8"?>
<project version="4">
  <component name="CopilotDiffPersistence">
    <option name="pendingDiffs">
      <map>
        <entry key="/OrderManager (snapshot).cs">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="/OrderManager (snapshot).cs" />
              <option name="originalContent" value="using UnityEngine;&#10;using System.Collections.Generic;&#10;using YesChef.Core;&#10;using YesChef.Data;&#10;using YesChef.Stations;&#10;&#10;namespace YesChef.Managers&#10;{&#10;    public class OrderManager : MonoBehaviour&#10;    {&#10;        [Header(&quot;Data References&quot;)] [SerializeField]&#10;        private IngredientData[] allAvailableIngredients;&#10;&#10;        [Header(&quot;Scene References&quot;)] [SerializeField]&#10;        private CustomerWindow[] customerWindows = new CustomerWindow[4];&#10;&#10;        private int currentTotalScore = 0;&#10;&#10;        private void OnEnable()&#10;        {&#10;            GameEvents.OnOrderCompleted += HandleOrderCompleted;&#10;        }&#10;&#10;        private void OnDisable()&#10;        {&#10;            GameEvents.OnOrderCompleted -= HandleOrderCompleted;&#10;        }&#10;&#10;        private void Start()&#10;        {&#10;            foreach (var window in customerWindows)&#10;            {&#10;                GenerateOrderForWindow(window);&#10;            }&#10;        }&#10;&#10;        private void HandleOrderCompleted(CustomerWindow window, int baseScore, float timeActive)&#10;        {&#10;            var timePenalty = Mathf.FloorToInt(timeActive);&#10;            var finalOrderScore = baseScore - timePenalty;&#10;&#10;            currentTotalScore += finalOrderScore;&#10;&#10;            GameEvents.OnScoreChanged?.Invoke(currentTotalScore);&#10;&#10;            Debug.Log($&quot;Great job! You completed an order. Base score was {baseScore}, but with a time penalty of {timePenalty}, your net score is {finalOrderScore}. Total score now: {currentTotalScore}.&quot;);&#10;&#10;            RespawnOrderAsync(window);&#10;        }&#10;&#10;        private async void RespawnOrderAsync(CustomerWindow window)&#10;        {&#10;            try&#10;            {&#10;                var timer = 0f;&#10;                while (timer &lt; 5f)&#10;                {&#10;                    timer += Time.deltaTime;&#10;                    await Awaitable.NextFrameAsync(destroyCancellationToken);&#10;                }&#10;&#10;                GenerateOrderForWindow(window);&#10;            }&#10;            catch (System.OperationCanceledException)&#10;            {&#10;            }&#10;        }&#10;&#10;        private void GenerateOrderForWindow(CustomerWindow window)&#10;        {&#10;            var ingredientCount = Random.value &gt; 0.5f ? 2 : 3;&#10;            var newOrder = new List&lt;IngredientData&gt;();&#10;&#10;            for (var i = 0; i &lt; ingredientCount; i++)&#10;            {&#10;                var randomIngredient = allAvailableIngredients[Random.Range(0, allAvailableIngredients.Length)];&#10;                newOrder.Add(randomIngredient);&#10;            }&#10;&#10;            window.StartNewOrder(newOrder);&#10;        }&#10;    }&#10;}" />
              <option name="updatedContent" value="using UnityEngine;&#10;using System.Collections.Generic;&#10;using YesChef.Core;&#10;using YesChef.Data;&#10;using YesChef.Stations;&#10;&#10;namespace YesChef.Managers&#10;{&#10;    public class OrderManager : MonoBehaviour&#10;    {&#10;        [Header(&quot;Data References&quot;)] [SerializeField]&#10;        private IngredientData[] allAvailableIngredients;&#10;&#10;        [Header(&quot;Scene References&quot;)] [SerializeField]&#10;        private CustomerWindow[] customerWindows = new CustomerWindow[4];&#10;&#10;        private int currentTotalScore = 0;&#10;&#10;        private void OnEnable()&#10;        {&#10;            GameEvents.OnOrderCompleted += HandleOrderCompleted;&#10;        }&#10;&#10;        private void OnDisable()&#10;        {&#10;            GameEvents.OnOrderCompleted -= HandleOrderCompleted;&#10;        }&#10;&#10;        private void Start()&#10;        {&#10;            foreach (var window in customerWindows)&#10;            {&#10;                GenerateOrderForWindow(window);&#10;            }&#10;        }&#10;&#10;        private void HandleOrderCompleted(CustomerWindow window, int baseScore, float timeActive)&#10;        {&#10;            var timePenalty = Mathf.FloorToInt(timeActive);&#10;            var finalOrderScore = baseScore - timePenalty;&#10;&#10;            currentTotalScore += finalOrderScore;&#10;&#10;            GameEvents.OnScoreChanged?.Invoke(currentTotalScore);&#10;&#10;            Debug.Log($&quot;Great job! You completed an order. Base score was {baseScore}, but with a time penalty of {timePenalty}, your net score is {finalOrderScore}. Total score now: {currentTotalScore}.&quot;);&#10;&#10;            RespawnOrderAsync(window);&#10;        }&#10;&#10;        private async void RespawnOrderAsync(CustomerWindow window)&#10;        {&#10;            var timer = 0f;&#10;            while (timer &lt; 5f)&#10;            {&#10;                timer += Time.deltaTime;&#10;                await Awaitable.NextFrameAsync(destroyCancellationToken);&#10;            }&#10;&#10;            GenerateOrderForWindow(window);&#10;        }&#10;&#10;        private void GenerateOrderForWindow(CustomerWindow window)&#10;        {&#10;            var ingredientCount = Random.value &gt; 0.5f ? 2 : 3;&#10;            var newOrder = new List&lt;IngredientData&gt;();&#10;&#10;            for (var i = 0; i &lt; ingredientCount; i++)&#10;            {&#10;                var randomIngredient = allAvailableIngredients[Random.Range(0, allAvailableIngredients.Length)];&#10;                newOrder.Add(randomIngredient);&#10;            }&#10;&#10;            window.StartNewOrder(newOrder);&#10;        }&#10;    }&#10;}" />
            </PendingDiffInfo>
          </value>
        </entry>
        <entry key="/Stove (snapshot).cs">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="/Stove (snapshot).cs" />
              <option name="originalContent" value="using System.Text;&#10;using TMPro;&#10;using UnityEngine;&#10;using YesChef.Core;&#10;using YesChef.Data;&#10;using YesChef.Player;&#10;&#10;namespace YesChef.Stations&#10;{&#10;    public class Stove : MonoBehaviour, IInteractable&#10;    {&#10;        [System.Serializable]&#10;        private class StoveSlot&#10;        {&#10;            public Transform holdPoint;&#10;            public TMP_Text timerText;&#10;            [HideInInspector] public Ingredient ingredient;&#10;            [HideInInspector] public bool isCooking;&#10;        }&#10;&#10;        [Header(&quot;Stove Settings&quot;)] [SerializeField]&#10;        private StoveSlot[] slots = new StoveSlot[2];&#10;&#10;        public bool TryInteract(PlayerInteractor player)&#10;        {&#10;            if (player.HeldIngredient == null)&#10;            {&#10;                foreach (var slot in slots)&#10;                {&#10;                    if (slot.ingredient != null &amp;&amp; !slot.isCooking &amp;&amp; slot.ingredient.CurrentState == IngredientState.Prepped)&#10;                    {&#10;                        player.SetHeldIngredient(slot.ingredient);&#10;                        slot.ingredient = null;&#10;                        return true;&#10;                    }&#10;                }&#10;            }&#10;&#10;            if (player.HeldIngredient != null &amp;&amp; player.HeldIngredient.Data.type == IngredientType.Meat &amp;&amp; player.HeldIngredient.CurrentState == IngredientState.Raw)&#10;            {&#10;                foreach (var slot in slots)&#10;                {&#10;                    if (slot.ingredient == null)&#10;                    {&#10;                        slot.ingredient = player.TakeHeldIngredient();&#10;                        slot.ingredient.transform.SetParent(slot.holdPoint);&#10;                        slot.ingredient.transform.localPosition = Vector3.zero;&#10;                        slot.ingredient.transform.localRotation = Quaternion.identity;&#10;&#10;                        CookMeatAsync(slot);&#10;                        return true;&#10;                    }&#10;                }&#10;            }&#10;&#10;            return false;&#10;        }&#10;&#10;        private async void CookMeatAsync(StoveSlot slot)&#10;        {&#10;            slot.isCooking = true;&#10;            var timer = 0f;&#10;            var prepTime = slot.ingredient.Data.prepTime;&#10;            var sb = new StringBuilder();&#10;&#10;            Debug.Log($&quot;I'm starting to cook the {slot.ingredient.Data.ingredientName}.&quot;);&#10;&#10;            try&#10;            {&#10;                while (timer &lt; prepTime)&#10;                {&#10;                    timer += Time.deltaTime;&#10;&#10;                    sb.AppendFormat(&quot;{0:0.0}s&quot;, prepTime - timer);&#10;                    slot.timerText.text = sb.ToString();&#10;                    sb.Clear();&#10;&#10;                    await Awaitable.NextFrameAsync(destroyCancellationToken);&#10;                }&#10;&#10;                slot.ingredient.SetState(IngredientState.Prepped);&#10;                Debug.Log($&quot;The {slot.ingredient.Data.ingredientName} is done cooking!&quot;);&#10;            }&#10;            catch (System.OperationCanceledException)&#10;            {&#10;            }&#10;            finally&#10;            {&#10;                slot.isCooking = false;&#10;            }&#10;        }&#10;    }&#10;}" />
              <option name="updatedContent" value="using System.Text;&#10;using TMPro;&#10;using UnityEngine;&#10;using YesChef.Core;&#10;using YesChef.Data;&#10;using YesChef.Player;&#10;&#10;namespace YesChef.Stations&#10;{&#10;    public class Stove : MonoBehaviour, IInteractable&#10;    {&#10;        [System.Serializable]&#10;        private class StoveSlot&#10;        {&#10;            public Transform holdPoint;&#10;            public TMP_Text timerText;&#10;            [HideInInspector] public Ingredient ingredient;&#10;            [HideInInspector] public bool isCooking;&#10;        }&#10;&#10;        [Header(&quot;Stove Settings&quot;)] [SerializeField]&#10;        private StoveSlot[] slots = new StoveSlot[2];&#10;&#10;        public bool TryInteract(PlayerInteractor player)&#10;        {&#10;            if (player.HeldIngredient == null)&#10;            {&#10;                foreach (var slot in slots)&#10;                {&#10;                    if (slot.ingredient != null &amp;&amp; !slot.isCooking &amp;&amp; slot.ingredient.CurrentState == IngredientState.Prepped)&#10;                    {&#10;                        player.SetHeldIngredient(slot.ingredient);&#10;                        slot.ingredient = null;&#10;                        return true;&#10;                    }&#10;                }&#10;            }&#10;&#10;            if (player.HeldIngredient != null &amp;&amp; player.HeldIngredient.Data.type == IngredientType.Meat &amp;&amp; player.HeldIngredient.CurrentState == IngredientState.Raw)&#10;            {&#10;                foreach (var slot in slots)&#10;                {&#10;                    if (slot.ingredient == null)&#10;                    {&#10;                        slot.ingredient = player.TakeHeldIngredient();&#10;                        slot.ingredient.transform.SetParent(slot.holdPoint);&#10;                        slot.ingredient.transform.localPosition = Vector3.zero;&#10;                        slot.ingredient.transform.localRotation = Quaternion.identity;&#10;&#10;                        CookMeatAsync(slot);&#10;                        return true;&#10;                    }&#10;                }&#10;            }&#10;&#10;            return false;&#10;        }&#10;&#10;        private async void CookMeatAsync(StoveSlot slot)&#10;        {&#10;            slot.isCooking = true;&#10;            var timer = 0f;&#10;            var prepTime = slot.ingredient.Data.prepTime;&#10;            var sb = new StringBuilder();&#10;&#10;            Debug.Log($&quot;I'm starting to cook the {slot.ingredient.Data.ingredientName}.&quot;);&#10;&#10;            try&#10;            {&#10;                while (timer &lt; prepTime)&#10;                {&#10;                    timer += Time.deltaTime;&#10;&#10;                    sb.AppendFormat(&quot;{0:0.0}s&quot;, prepTime - timer);&#10;                    slot.timerText.text = sb.ToString();&#10;                    sb.Clear();&#10;&#10;                    await Awaitable.NextFrameAsync(destroyCancellationToken);&#10;                }&#10;&#10;                slot.ingredient.SetState(IngredientState.Prepped);&#10;                Debug.Log($&quot;The {slot.ingredient.Data.ingredientName} is done cooking!&quot;);&#10;            }&#10;            finally&#10;            {&#10;                slot.isCooking = false;&#10;            }&#10;        }&#10;    }&#10;}" />
            </PendingDiffInfo>
          </value>
        </entry>
        <entry key="/Table (snapshot).cs">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="/Table (snapshot).cs" />
              <option name="originalContent" value="using System;&#10;using System.Text;&#10;using TMPro;&#10;using UnityEngine;&#10;using YesChef.Core;&#10;using YesChef.Player;&#10;using YesChef.Data;&#10;&#10;namespace YesChef.Stations&#10;{&#10;    public class Table : MonoBehaviour, IInteractable&#10;    {&#10;        [Header(&quot;Table Settings&quot;)] [SerializeField]&#10;        private Transform placementPoint;&#10;&#10;        [SerializeField] private TMP_Text timerTxt;&#10;&#10;        private Ingredient currentIngredient;&#10;        private bool isProcessing;&#10;&#10;        public bool TryInteract(PlayerInteractor player)&#10;        {&#10;            if (currentIngredient == null &amp;&amp; player.HeldIngredient != null)&#10;            {&#10;                if (player.HeldIngredient.Data.type == IngredientType.Vegetable &amp;&amp; player.HeldIngredient.CurrentState == IngredientState.Raw)&#10;                {&#10;                    currentIngredient = player.TakeHeldIngredient();&#10;                    currentIngredient.transform.SetParent(placementPoint);&#10;                    currentIngredient.transform.localPosition = Vector3.zero;&#10;                    currentIngredient.transform.localRotation = Quaternion.identity;&#10;&#10;                    ProcessIngredientAsync();&#10;                    return true;&#10;                }&#10;            }&#10;&#10;            if (currentIngredient != null &amp;&amp; !isProcessing &amp;&amp; player.HeldIngredient == null)&#10;            {&#10;                if (currentIngredient.CurrentState == IngredientState.Prepped)&#10;                {&#10;                    player.SetHeldIngredient(currentIngredient);&#10;                    currentIngredient = null;&#10;                    return true;&#10;                }&#10;            }&#10;&#10;            return false;&#10;        }&#10;&#10;        private async void ProcessIngredientAsync()&#10;        {&#10;            isProcessing = true;&#10;            var timer = 0f;&#10;            var prepTime = currentIngredient.Data.prepTime;&#10;            var sb = new StringBuilder();&#10;            Debug.Log($&quot;I'm chopping the {currentIngredient.Data.ingredientName} now.&quot;);&#10;&#10;            try&#10;            {&#10;                while (timer &lt; prepTime)&#10;                {&#10;                    timer += Time.deltaTime;&#10;                    sb.AppendFormat(&quot;{0:0.0}s&quot;, prepTime - timer);&#10;                    timerTxt.text = sb.ToString();&#10;                    sb.Clear();&#10;&#10;                    await Awaitable.NextFrameAsync(destroyCancellationToken);&#10;                }&#10;&#10;                currentIngredient.SetState(IngredientState.Prepped);&#10;                Debug.Log($&quot;Chopping done! The {currentIngredient.Data.ingredientName} is ready.&quot;);&#10;            }&#10;            catch (OperationCanceledException)&#10;            {&#10;            }&#10;            finally&#10;            {&#10;                isProcessing = false;&#10;            }&#10;        }&#10;    }&#10;}" />
              <option name="updatedContent" value="using System;&#10;using System.Text;&#10;using TMPro;&#10;using UnityEngine;&#10;using YesChef.Core;&#10;using YesChef.Player;&#10;using YesChef.Data;&#10;&#10;namespace YesChef.Stations&#10;{&#10;    public class Table : MonoBehaviour, IInteractable&#10;    {&#10;        [Header(&quot;Table Settings&quot;)] [SerializeField]&#10;        private Transform placementPoint;&#10;&#10;        [SerializeField] private TMP_Text timerTxt;&#10;&#10;        private Ingredient currentIngredient;&#10;        private bool isProcessing;&#10;&#10;        public bool TryInteract(PlayerInteractor player)&#10;        {&#10;            if (currentIngredient == null &amp;&amp; player.HeldIngredient != null)&#10;            {&#10;                if (player.HeldIngredient.Data.type == IngredientType.Vegetable &amp;&amp; player.HeldIngredient.CurrentState == IngredientState.Raw)&#10;                {&#10;                    currentIngredient = player.TakeHeldIngredient();&#10;                    currentIngredient.transform.SetParent(placementPoint);&#10;                    currentIngredient.transform.localPosition = Vector3.zero;&#10;                    currentIngredient.transform.localRotation = Quaternion.identity;&#10;&#10;                    ProcessIngredientAsync();&#10;                    return true;&#10;                }&#10;            }&#10;&#10;            if (currentIngredient != null &amp;&amp; !isProcessing &amp;&amp; player.HeldIngredient == null)&#10;            {&#10;                if (currentIngredient.CurrentState == IngredientState.Prepped)&#10;                {&#10;                    player.SetHeldIngredient(currentIngredient);&#10;                    currentIngredient = null;&#10;                    return true;&#10;                }&#10;            }&#10;&#10;            return false;&#10;        }&#10;&#10;        private async void ProcessIngredientAsync()&#10;        {&#10;            isProcessing = true;&#10;            var timer = 0f;&#10;            var prepTime = currentIngredient.Data.prepTime;&#10;            var sb = new StringBuilder();&#10;            Debug.Log($&quot;I'm chopping the {currentIngredient.Data.ingredientName} now.&quot;);&#10;&#10;            try&#10;            {&#10;                while (timer &lt; prepTime)&#10;                {&#10;                    timer += Time.deltaTime;&#10;                    sb.AppendFormat(&quot;{0:0.0}s&quot;, prepTime - timer);&#10;                    timerTxt.text = sb.ToString();&#10;                    sb.Clear();&#10;&#10;                    await Awaitable.NextFrameAsync(destroyCancellationToken);&#10;                }&#10;&#10;                currentIngredient.SetState(IngredientState.Prepped);&#10;                Debug.Log($&quot;Chopping done! The {currentIngredient.Data.ingredientName} is ready.&quot;);&#10;            }&#10;            finally&#10;            {&#10;                isProcessing = false;&#10;            }&#10;        }&#10;    }&#10;}" />
            </PendingDiffInfo>
          </value>
        </entry>
      </map>
    </option>
  </component>
</project>